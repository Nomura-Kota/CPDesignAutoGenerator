<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- 出力先フォルダ -->
  <property name="LOG_DIR" value="C:/com/example/CPDesignAutoGenerator/log"/>

  <!-- コンソール -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ロールするファイル: 10MB or 日次で切替、最大30世代 -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/CPDesignAutoGenerator.log</file>
    <immediateFlush>true</immediateFlush>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger - %msg%n</pattern>
    </encoder>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/CPDesignAutoGenerator-%d{yyyy-MM-dd}.%i.log.zip</fileNamePattern>
      <maxFileSize>10MB</maxFileSize>
      <maxHistory>30</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>
  </appender>

  <shutdownHook class="ch.qos.logback.core.hook.DelayingShutdownHook"/>

  <!-- ルートロガー -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="FILE"/>
  </root>

</configuration>
